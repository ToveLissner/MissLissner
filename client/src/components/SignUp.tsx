import { Button } from "@mui/material";

const SignUp = () => {
  return (
    <Button
      variant="contained"
      color="success"
      sx={{ textTransform: "none", fontSize: "14px" }}
    >
      Öppna konto
    </Button>
  );
};

export default SignUp;
